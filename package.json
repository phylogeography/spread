{
  "scripts": {
    "lint": "clj-kondo --config .clj-kondo/config.edn --lint src/cljs",
    "release": "yarn shadow-cljs release ui",
    "watch": "shadow-cljs watch ui",
    "clean": "git clean -fdx",
    "deps": "yarn install --frozen-lockfile"
  },
  "devDependencies": {
    "clj-kondo": "2020.11.7",
    "shadow-cljs": "^2.11.15"
  },
  "dependencies": {
    "@sentry/browser": "^6.1.0",
    "axios": "^0.21.1",
    "react": "17.0.1",
    "react-dom": "17.0.1"
  }
}
