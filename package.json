{
  "scripts": {
    "release": "yarn shadow-cljs release ui",
    "watch": "shadow-cljs watch ui",
    "clean": "git clean -fdx",
    "deps": "yarn install --frozen-lockfile"
  },
  "devDependencies": {
    "shadow-cljs": "^2.11.15"
  },
  "dependencies": {
    "react": "17.0.1",
    "react-dom": "17.0.1"
  }
}
